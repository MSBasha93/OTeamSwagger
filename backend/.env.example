# --- General ---
NODE_ENV=development
PORT=3001

# --- Database (PostgreSQL) ---
# For local development with docker-compose.yml
DATABASE_URL="postgresql://oteam_user:oteam_password@localhost:5432/oteam_db?schema=public"

# --- JWT Authentication ---
JWT_SECRET=your-super-secret-jwt-key-for-dev
JWT_EXPIRATION_TIME=3600s # e.g., 3600s (1 hour), 1d (1 day)

# --- AI Service (Placeholder) ---
AI_SERVICE_API_KEY=your_ai_service_key_if_needed
AI_SERVICE_ENDPOINT=http://localhost:5005/webhooks/rest/webhook # Example for Rasa

# --- ScreenConnect (Placeholder) ---
SCREENCONNECT_API_URL=your_screenconnect_api_url
SCREENCONNECT_API_KEY=your_screenconnect_api_key